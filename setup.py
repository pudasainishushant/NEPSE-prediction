from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This is a time series forecasting project used to predict the next Nepal Stock Exchange(NEPSE) values. This is done by the extensive use of LSTM newtorks.',
    author='ACEM',
    license='MIT',
)
